# 稳定性补丁与上下文查看器合并记录

## 来源与边界

- 来源：`D:/AI/simingdevelop1`，`main@ad56db35d3677685afe0d93296b544206e07f5ae` 之上的未提交改动。
- 目标：`D:/AI/simingdevelop` 的 `codex/context-inspector`，合并前为 `4979fbf`。
- 本批为 43 文件快照；两次完整内容核对间隔 102 秒，HEAD、文件清单和 SHA-256 指纹一致。
- 来源快照提交：`532fff27bcf90f5d10b79b845ec10d96359f921d`。
- 来源指纹：`cebf2adf0c8bd743395939375d6b14d3c710cd067bc3a4023e0291a1c7663ba3`。

来源目录仍在编辑。本次只读取其 Git 信息与工作文件，没有修改来源文件、索引、分支或提交，也没有导入运行数据、密钥、日志或安装产物。后续增量通过同一任务的定时检查继续核对。

## 合并内容

1. 立项写入回执按中央契约投影嵌套元数据，避免把完整阶段正文放进小回执，进而将真实写入成功错误地反馈为容量失败。
2. 地点、势力与其他实体生成共用结构契约，保留字段路径和错误码，由同一模型修正无效结构。
3. 首版卷实体生成保留必需的主线字段；回执提供已保存、待确认、revision 和真实集合数量。
4. 开篇细纲要求真实卷 ID、明确章节与场景父子关联及非空正文，保存和作品落地使用同一校验。Android 同步独立生成、保存及落地契约。

工具结果投影文件有一处导入冲突，合并后同时保留生成诊断和上下文采集。Android 的模型可见回执在实际投影后采集，工具步骤范围包括持久化与结果投影；新增断言比对查看器记录与真实 HTTP 请求中的回执，确认原始大正文和精简回执分层保存。

来源快照中的生成校验循环导入拆至 `generation_errors.py`，错误类与诊断文本各有一个权威实现。移除无关的导入换行增长以保持既有模块大小门禁。旧的紧凑立项测试改为提供明确的真实卷引用，不再假定模板可以自动选择父节点。

## 验证

- 后端 55 个相关测试模块，共 802 项通过、3 项条件跳过；其中专项 128 项覆盖新生成契约、回执、上下文记录与移动端共享契约。紧凑立项旧样例调整后单独复测 21 项通过。
- Android 全部 315 项单元测试及 `lintDebug` 通过，包括大正文改名仅持久化一次、成功总结、记录的精简回执与实际请求一致。
- 后端架构、Prompt 编译与预算、规定范围的 Ruff 和 4 项导入边界检查通过；循环依赖为 0。
- 前端架构、重复率门禁和 OpenAPI 类型一致性检查通过。
- 本轮测试只使用合成输入、临时数据库和模拟供应商，没有请求真实模型或修改用户业务数据。

同目录另外三篇来源 QA 文档中的安装包路径和哈希是来源目录的历史验证记录。本次交付为代码合并，之前的上下文查看器安装包对应 `4979fbf`，并未因合并自动更新。

## 持续跟踪

已设置每 15 分钟检查一次。来源的新提交与未提交内容均需形成稳定、可回溯的快照，再三方合并和验证。目标目录有其他未提交工作、分支切换或验证失败时保留现场；不覆盖正在编辑的内容。检查无变化时不发送重复进度；完成合并或需要处理问题时报告结果。

本机监测状态、快照清单和辅助脚本保存在目标仓库 `.git/codex-patch-monitor`；最近已合入的来源快照使用 `refs/codex/simingdevelop1/integrated` 标记，后续同步据此计算增量。未授权自动推送、发布或重复打包。
