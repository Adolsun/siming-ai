# 立项 Patch 参数失败与恢复检查

## 现场记录

2026-09-09 的立项对话记录保留了 direct_mcp_calls。读取快照后，模型连续三次调用 patch_creation_artifact，目标为 concepts、expected_revision 为 0；三次 changes 均为字符串，返回 $.changes / list_type。修订版本没有增加。模型改变了字符串中的内容和转义，但没有纠正字段类型。

权威 MCP Schema 已要求 changes 为原生数组；没有发现系统把数组转换成字符串的证据。此次不把参数错误解释为作者资料损坏。

## 已修复的确定性问题

- 参数诊断的模型投影明确显示安全字段路径和校验规则。list_type 错误直接说明必须传 `[...]`，修改字符串内容或转义不能解决。
- 立项原生执行路径复用同一参数诊断投影，不再将具体参数错误折叠为泛化执行失败。
- 三次失败事件携带经过清理的最后一次失败原因；最终回复说明真实错误，不再使用“检查当前资料结构、选择单一对象”的固定误导性提示。
- 阶段和实体 Patch 的权威 Schema 补充原生数组示例。
- 保留三次失败上限、权限与结构校验。没有解析字符串后静默代写，也没有替模型选择创意方向。

## 离线验证

- 实际 MCP 执行入口：字符串 changes 返回 list_type，资料修订版本保持不变；模型模拟器改传原生数组后，同一次修改成功，版本仅增加一次。
- 三次失败事件及最终回复保留 $.changes 和数组修复指引，不回显原始异常文字。
- 参数恢复、类别控制、结果投影与 MCP 测试：61 项通过，3 项环境条件跳过。
- 立项 Agent、MCP 工具和移动契约相关回归：74 项通过。
- 手机/PC 能力契约及移动提示词导出检查通过；没有手机真机验收。
- 日志：`.build/creation-patch-recovery.log`、`.build/creation-agent-recovery-regression.log`。

没有调用真实业务模型，没有修改用户立项内容。测试证明契约和错误反馈可用，不保证真实模型下一次一定纠正结构。

## 测试安装包

- 路径：`release/local-creation-patch-20260909/Siming-Setup.exe`，包含此前修复及本轮立项参数反馈修复。
- 版本：3.3.13；大小：42,581,469 字节。
- SHA-256：`8ed277911afc9f74fa0ed71f18287a3abe180999e9b986b4d605dc8685fc98b5`
- 正式构建、打包 MCP 冒烟检查（153 个工具）和安装包哈希校验通过。
- 构建日志：`.build/creation-patch-installer.log`。
